rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 로그인(팀 공용 계정)한 사람만 읽기/쓰기 가능.
    // Firebase 콘솔 > Firestore Database > 규칙 탭에 이 내용을 그대로 붙여넣고 게시하세요.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
